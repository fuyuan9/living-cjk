{
  "name": "living-cjk-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --filter demo dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "tsup": "^8.3.6"
  }
}
